---
name: GuideCard
---
export const GuideCard = ({
    /* icon class in font-awesome */
    icon,
    /* card title */
    title,
    /* card description */
    description,
    /* card icon color*/
    iconColor,
    /* background icon color */
    bgIconColor,
}) => {
			return (
        <div className="pp-guide-card">
            <div 
                className="pp-guide-card__icon"
                style={bgIconColor ? { backgroundColor: bgIconColor } : {}}
            >
                <i 
                    className={`pp-guide-card__icon--img ${icon}`} 
                    style={iconColor ? { color: iconColor } : {}}
                    aria-hidden="true" 
                />
            </div>
            <div className="pp-guide-card__content">
                <h3 className="pp-guide-card__title">{title}</h3>
                <p className="pp-guide-card__description">{description}</p>
            </div>
        </div>
    )
}

<GuideCard
    icon="fa-solid fa-circle-info"
    title="Guide Card Title"
    description="This is a description of the guide card."
    iconColor="#ffffff"
    bgIconColor="#182230"
/>